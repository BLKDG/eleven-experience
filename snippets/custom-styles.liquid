<style>
  {%- comment -%}
    Custom font overrides for Eleven Experience
    Using Larken for headlines and Gotham for sub-headings and body text
    Adobe Fonts kit: https://use.typekit.net/pbb5cjw.css
  {%- endcomment -%}

  :root {
    /* Larken font for all headlines (banner, featured, section, block headings) */
    --font--heading--family: "larken", serif;

    /* Gotham font for sub-headings, body text, navigation, and buttons */
    --font--paragraph--family: "gotham", sans-serif;
    --font--nav--family: "gotham", sans-serif;
    --font--nav--weight: 600;
  }

  /* Ensure Gotham is applied to all body text, navigation, buttons, and sub-headings */
  body,
  [data-item="paragraph"],
  [data-item="featured-paragraph"],
  [data-item="nav-text"],
  [data-item="button"],
  [data-item="overline"],
  [data-item="block-heading"] {
    font-family: var(--font--paragraph--family) !important;
  }

  /* Ensure Larken is applied to all headline types */
  [data-item="banner-heading"],
  [data-item="featured-heading"],
  [data-item="section-heading"] {
    font-family: var(--font--heading--family) !important;
  }

  h1, h2, h3, h4, h5, h6 {
    font-family: var(--font--heading--family) !important;
  }

  /* Mobile video aspect ratio override */
  @media only screen and (max-width: 767px) {
    .video--root {
      --natural-aspect-ratio: var(--natural-mobile-aspect-ratio, 0.775) !important;
    }
  }

  .footer-bar--text {
    text-transform: capitalize;
    font-size: .75rem;
    font-weight: 100;
  }
</style>